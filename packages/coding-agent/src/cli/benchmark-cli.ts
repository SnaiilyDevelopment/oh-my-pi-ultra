import * as fs from "node:fs/promises";
import * as path from "node:path";
import { DEFAULT_REGRESSION_THRESHOLDS, compareRecords } from "../benchmark/metrics";
import { renderComparison, summarizeRunSet } from "../benchmark/report";
import { newRunSetId, runBenchmarkTask, validateBenchmarkEnvironment } from "../benchmark/runner";
import { BenchmarkStore } from "../benchmark/storage";
import { BENCHMARK_CATEGORIES, BENCHMARK_VARIANTS, type BenchmarkRunRecord, type BenchmarkRunSet, type BenchmarkTaskSuite, type BenchmarkVariant, type RegressionThresholds } from "../benchmark/types";
export interface BenchmarkCommandArgs{action:string;target?:string;flags:{suite?:string;repository?:string;tasks?:string;variant?:string;baselineCommand?:string;ultraCommand?:string;runs?:number;timeout?:number;outputDir?:string;requireTools?:string;model?:string;provider?:string;ompCommit?:string;ompUltraCommit?:string;branch?:string;json?:boolean;successThreshold?:string;tokenThreshold?:string;latencyThreshold?:string;modelCallThreshold?:string;toolCallThreshold?:string;};}
const SEED_SUITE=path.join(import.meta.dir,"..","benchmark","seed-suite.json");
const positive=(value:number|undefined,fallback:number,label:string)=>{if(value===undefined)return fallback;if(!Number.isInteger(value)||value<=0)throw new Error(`Expected --${label} to be a positive integer, got ${value}`);return value};
const ratio=(value:string|undefined,fallback:number,label:string)=>{if(value===undefined)return fallback;const parsed=Number(value);if(!Number.isFinite(parsed)||parsed<0)throw new Error(`Expected --${label} to be a non-negative number, got ${value}`);return parsed};
export function validateBenchmarkSuite(suite:BenchmarkTaskSuite):void{if(!suite||typeof suite.name!=="string"||!Array.isArray(suite.tasks)||suite.tasks.length===0)throw new Error("invalid benchmark suite: expected a non-empty tasks array");const ids=new Set<string>();for(const task of suite.tasks){if(!task.id||ids.has(task.id))throw new Error(`invalid or duplicate benchmark task id: ${task.id}`);ids.add(task.id);if(!BENCHMARK_CATEGORIES.includes(task.category))throw new Error(`invalid benchmark category for ${task.id}: ${task.category}`);if(!task.prompt||!task.expectedOutcome||!task.verification)throw new Error(`invalid benchmark task: ${task.id}`);if(!task.timeout||task.timeout<=0)throw new Error(`invalid timeout for ${task.id}`);}}
export async function loadBenchmarkSuite(file:string):Promise<BenchmarkTaskSuite>{const suite=JSON.parse(await fs.readFile(path.resolve(file),"utf8")) as BenchmarkTaskSuite;validateBenchmarkSuite(suite);return suite;}
function selectTasks(suite:BenchmarkTaskSuite,filter?:string){if(!filter)return suite.tasks;const wanted=new Set(filter.split(",").map(v=>v.trim()).filter(Boolean));const selected=suite.tasks.filter(t=>wanted.has(t.id));const missing=[...wanted].filter(id=>!suite.tasks.some(t=>t.id===id));if(missing.length)throw new Error(`unknown benchmark task(s): ${missing.join(", ")}`);return selected;}
function selectVariants(value?:string):BenchmarkVariant[]{const requested=value??"both";if(requested==="both")return["baseline","ultra"];if(!BENCHMARK_VARIANTS.includes(requested as BenchmarkVariant))throw new Error(`invalid benchmark variant: ${requested}`);return[requested as BenchmarkVariant];}
function thresholds(flags:BenchmarkCommandArgs["flags"]):RegressionThresholds{return{successRateDelta:ratio(flags.successThreshold,DEFAULT_REGRESSION_THRESHOLDS.successRateDelta,"success-threshold"),tokenIncreaseRatio:ratio(flags.tokenThreshold,DEFAULT_REGRESSION_THRESHOLDS.tokenIncreaseRatio,"token-threshold"),latencyIncreaseRatio:ratio(flags.latencyThreshold,DEFAULT_REGRESSION_THRESHOLDS.latencyIncreaseRatio,"latency-threshold"),modelCallIncreaseRatio:ratio(flags.modelCallThreshold,DEFAULT_REGRESSION_THRESHOLDS.modelCallIncreaseRatio,"model-call-threshold"),toolCallIncreaseRatio:ratio(flags.toolCallThreshold,DEFAULT_REGRESSION_THRESHOLDS.toolCallIncreaseRatio,"tool-call-threshold")};}
const loadRecords=(store:BenchmarkStore,ids:string[])=>Promise.all(ids.map(id=>store.loadRun(id)));
export async function runBenchmarkCommand(args:BenchmarkCommandArgs):Promise<unknown>{const store=new BenchmarkStore(path.resolve(args.flags.outputDir??".omp/benchmarks"));await store.init();if(args.action==="list"){const suite=await loadBenchmarkSuite(args.flags.suite??SEED_SUITE);return{suite:suite.name,version:suite.version,taskCount:suite.tasks.length,tasks:suite.tasks.map(t=>({id:t.id,category:t.category,difficulty:t.difficulty,description:t.description,tags:t.tags}))};}if(args.action==="task"){const suite=await loadBenchmarkSuite(args.flags.suite??SEED_SUITE);const task=suite.tasks.find(t=>t.id===args.target);if(!task)throw new Error(`unknown benchmark task: ${args.target}`);return task;}if(args.action==="replay"){const record=await store.loadRun(args.target??"");return{runId:record.runId,taskId:record.taskId,variant:record.variant,repositoryCommit:record.environment.repositoryCommit,branch:record.environment.branch,model:record.environment.model,provider:record.environment.provider,commandProfile:record.reproducibility.commandProfile,taskFingerprint:record.reproducibility.taskFingerprint,probabilisticModelRun:record.reproducibility.probabilisticModelRun,message:"Replay requires restoring the recorded repository commit, model/settings, benchmark task, and runner command. Secrets are never persisted."};}if(args.action==="report"){const set=await store.loadRunSet(args.target??"");const records=await loadRecords(store,set.runs);const report=set.variants.includes("baseline")&&set.variants.includes("ultra")?compareRecords(records.filter(r=>r.variant==="baseline"),records.filter(r=>r.variant==="ultra"),set.runSetId,set.runSetId,thresholds(args.flags)):undefined;return args.flags.json?(report??{runSet:set,summary:summarizeRunSet(records)}):report?renderComparison(report):summarizeRunSet(records);}if(args.action==="compare"){const [baselineId,ultraId]=(args.target??"").split(",").map(v=>v.trim());if(!baselineId||!ultraId)throw new Error("compare requires <baseline-run-set>,<ultra-run-set>");const baselineSet=await store.loadRunSet(baselineId),ultraSet=await store.loadRunSet(ultraId),report=compareRecords(await loadRecords(store,baselineSet.runs),await loadRecords(store,ultraSet.runs),baselineSet.runSetId,ultraSet.runSetId,thresholds(args.flags));await store.saveReport(`${baselineSet.runSetId}-vs-${ultraSet.runSetId}`,report);return args.flags.json?report:renderComparison(report);}if(args.action!=="run")throw new Error(`unknown benchmark action: ${args.action}`);const suitePath=args.flags.suite??SEED_SUITE,suite=await loadBenchmarkSuite(suitePath),tasks=selectTasks(suite,args.flags.tasks),variants=selectVariants(args.flags.variant),runsPerTask=positive(args.flags.runs,1,"runs"),timeoutMs=positive(args.flags.timeout,900000,"timeout"),repoCandidates=[...new Set(tasks.map(t=>t.repository).filter(Boolean))],repository=path.resolve(args.flags.repository??repoCandidates[0]??process.cwd());if(!args.flags.repository&&repoCandidates.length>1)throw new Error("selected tasks reference multiple repositories; pass --repository explicitly");const requiredTools=(args.flags.requireTools??"").split(",").map(v=>v.trim()).filter(Boolean),envErrors=await validateBenchmarkEnvironment(repository,requiredTools);if(envErrors.length)throw new Error(`benchmark environment invalid:\n- ${envErrors.join("\n- ")}`);const runSetId=newRunSetId(),runIds:string[]=[];for(const variant of variants)for(const task of tasks){const command=variant==="baseline"?(args.flags.baselineCommand??task.commands?.baseline):(args.flags.ultraCommand??task.commands?.ultra);if(!command)throw new Error(`no ${variant} runner command configured for task ${task.id}`);for(let attempt=1;attempt<=runsPerTask;attempt++){const record=await runBenchmarkTask(task,{variant,command,repository,runsRoot:store.root,runSetId,attempt,timeoutMs:Math.min(timeoutMs,task.timeout),model:args.flags.model,provider:args.flags.provider,ompCommit:args.flags.ompCommit,ompUltraCommit:args.flags.ompUltraCommit,branch:args.flags.branch,store});await store.saveRun(record);runIds.push(record.runId);}}const set:BenchmarkRunSet={version:1,runSetId,createdAt:new Date().toISOString(),suiteName:suite.name,suiteVersion:suite.version,tasks:tasks.map(t=>t.id),variants,runs:runIds,config:{runsPerTask,timeoutMs,repositoryOverride:args.flags.repository,model:args.flags.model,provider:args.flags.provider,suitePath:path.resolve(suitePath)}};await store.saveRunSet(set);await store.saveTaskSuite(suite);const records=await loadRecords(store,runIds);return args.flags.json?{runSet:set,summary:summarizeRunSet(records)}:`Run set ${runSetId}\n${summarizeRunSet(records)}`;}
